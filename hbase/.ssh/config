# SSH configuration for dhd hbase
# Include this from host-specific SSH config files

Host *
    HashKnownHosts yes

    # Use 1password agent when SSH_1P_AGENT is set
    # This is set in shell precmd when not logged in over SSH
    Match exec "test -n $SSH_1P_AGENT"
        IdentityAgent $SSH_1P_AGENT
