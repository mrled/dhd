screen: C-z is now my screen cmd character
screen: C-z S = split screen
screen: C-z [tab] = change focus of screen
screen: C-z X = kill the current region (no-op if there is only one region)
screen: resize +N       increase current region height by N
screen: resize -N       decrease current region height by N
screen: resize  N       set current region height to N
screen: resize  =       make all windows equally high
screen: resize  max     maximize current region height
screen: resize  min     minimize current region height
irssi: /rmreconns keeps irssi from trying to connect to non-responding servers.
svn: svn co svn+ssh://mrled@mrled.org/home/.arietty/mrled/svn/dhd
dpkg: run "apt-get clean" to clear out the large /sw/var/cache directory
dpkg: "apt-get update" resyncs package index
dpkg: "apt-get dist-upgrade" (fink update-all) updates all installed packages. 
cdrecord: mac os x, for combo or super drives, dev=IODVDServices (/0?)
mac os x: hdiutil convert /path/to/filename.dmg -format UDTO -o /path/to/x.iso
ssh: ssh-keygen -t rsa; scp ~/.ssh/id_rsa.pub user@host:~/id_mj.pub
svn: setenv LC_ALL en_US # so that I dont get erros about converting to UTF-8
synergy: xmodmap switch CAPS & ctrl on server to switch on all clients. 
synergy: see the Configuration page to make Sun Type 6 kbd work 
mac os x: qs is the quicksilver command line client
zcli: /usr/opt/surfraw to surf in the raw
cli: yncs is yubnub. also installed as yubnub in some places, and in surfraw
svn: 'svn co -r # <repo>', where # is a revision number and <repo> is a repo
rsync: rsync -rvlu --progress --bwlimit=20 ~/USB.80GB.01/Software/ mrled@mrled.org:/home/mrled/UNIX/
cygwin: `cygstart` is like `open` under os x or `start` under cmd
screen: copy mode: C-z [
gentoo: eix is what you use for fast searches
gentoo: portage: eix-sync does emerge --sync and eix-update
hx: to connect to higher intellect, use /server -n vlack -f preterhuman.net
interix: /Tools packages: install: pkg_update -L $pkgname
interix: /Tools packages: update:  pkg_update -Ln OR pkg_update -La
mac os x: umount: use `hdiutil detach` or `drutil eject`
cygwin: ssh: DOMAIN\\username@host
serial ports: cu: `sudo cu -l /dev/ttyS1 -s 9600`. ~# sends break, ~. disconnects
emacs: delete not deleting? M-x normal-erase-is-backspace-mode

