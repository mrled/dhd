#!/bin/sh
set -eu



usage() {
    echo <<EOF
Usage: $cmdname <TITLE>
Set the title of the current tab in macOS Terminal.app
EOF
}

if test $# -ne 1; then
    usage
    exit 1
fi

echo -n -e "\033]0;$1\007"
